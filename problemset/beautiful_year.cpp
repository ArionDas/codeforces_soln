#include<bits/stdc++.h>
using namespace std;

bool check(int m) {
    
}

int main () {
    int y;
    cin >> y;

    bool b=false;

    for(int i=y; b=false; i++) {
        if(check(i))
            b=true;
    }
}